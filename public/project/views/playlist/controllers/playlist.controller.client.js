(function () {
    angular
        .module("rhythmShark")
        .controller("playlistController", playlistController);

    function playlistController($location, playlistService) {
        var model = this;

        function init() {

        }

        init();
    }

})();